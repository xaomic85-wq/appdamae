name: Criar APK
on: [push]
jobs:
  build:
  runs-on: ubuntu-latest
  steps
  - uses: actions/checkout@v4
  - uses: actions/setup-python@v5
  with:
python-version: '3.10'
- name: Build APK
uses
ArtemSBulgakov/buildozer-action@v1
id: buildozer:
command: buildozer android debug
buildozer_version:stable
-name:Upload actions/upload-artifact@v4
with:
name:app-da-mae-apk
pat$ {{ steps.buildozer.outputs. filename }}






:

      
